package frc.robot;

public class Autonomous{

/*
    public void autoDriveOnly() {
  
        if (m_timer.get() < 10.0) { 
            m_robotDrive.arcadeDrive(0.8, 0); // drive forwards half speed
        } else if (m_timer.get() < 10.0) {
            m_robotDrive.arcadeDrive(.4, 0);
        } else {
            m_robotDrive.stopMotor(); // stop robot
        }
    }
*/
}